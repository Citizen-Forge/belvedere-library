id: core/disk
name: Disk
root: hardware
extends: core/component
icon: disk
description: An individual storage disk, hosted by the server/enclosure it's installed in (HOSTS).
version: 0.1.0
attributes:
  - key: capacityGb
    label: Capacity
    dataType: number
    unit: GB
  - key: mediaType
    label: Media type
    dataType: enum
    options: [hdd, ssd, nvme]
  - key: role
    label: Role
    dataType: enum
    options: [data, parity, cache, boot, unassigned]
  - key: serialNumber
    label: Serial number
    dataType: string
  - key: pool
    label: Pool / slot name
    dataType: string
    required: false
